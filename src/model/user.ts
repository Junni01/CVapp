//This is the user inteface that is evoked in the login page and used in gathering the information from the text boxes and passed to firebase login. Saku Junni 1700009

export interface User {
    email: string;
    password: string;
}